# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
one:
  email: fixture1@example.com
  username: fixture_user_one
  display_name: Fixture One
  role: player
  total_points: 0
  daily_streak: 0
  achievements_count: 0
  encrypted_password: <%= Devise::Encryptor.digest(User, "Passwort123!") %>

two:
  email: fixture2@example.com
  username: fixture_user_two
  display_name: Fixture Two
  role: player
  total_points: 0
  daily_streak: 0
  achievements_count: 0
  encrypted_password: <%= Devise::Encryptor.digest(User, "Passwort123!") %>
